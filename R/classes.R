## make virtual classes to allow S4 style as(X, "im") etc
if (!isClass("ppp")) setClass("ppp")
if (!isClass("psp")) setClass("psp")
if (!isClass("owin")) setClass("owin")
if (!isClass("im")) setClass("im")
if (!isClass("tess")) setClass("tess")
if (!isClass("linnet")) setClass("linnet")

## if (!isClass("SpatRaster")) setClass("SpatRaster")

## if (!isClass("sf")) setClass("sf")

## if (!isClass("Line")) setClass("Line")
## if (!isClass("Lines")) setClass("Lines")
## if (!isClass("SpatialLines")) setClass("SpatialLines")
## if (!isClass("SpatialLinesDataFrame")) setClass("SpatialLinesDataFrame")
## if (!isClass("SpatialGridDataFrame")) setClass("SpatialGridDataFrame")
## if (!isClass("SpatialPolygons")) setClass("SpatialPolygons")
## if (!isClass("SpatialPoints")) setClass("SpatialPoints")
## if (!isClass("SpatialPointsDataFrame")) setClass("SpatialPointsDataFrame")
